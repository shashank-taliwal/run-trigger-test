pipelines:
  - name: first
    steps:
      - name: step1
        type: Bash
        configuration:
          integrations:
            - name: gits
          runtime:
            type: host
        execution:
          onExecute:
            - echo "Hello World"
